__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
# data.json is often user specific, but user might want to keep it? 
# Usually we ignore actual data. I'll ask user? No, standard is ignore.
# But for this one-shot project user might want it.
# I will NOT ignore data.json for now as it holds the state user tested.
